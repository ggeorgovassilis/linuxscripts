# tor-socks-proxy

Quickls & easily run a SOCKS tor proxy in docker.

## building
`build.sh`

## running
`run.sh`

You might want to modify run.sh to map tor to a different port. The default is 8080.
